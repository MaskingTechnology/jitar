{
    "name": "jitar-construction-example",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "rm -rf .jitar && tsc && jitar build",
        "standalone": "jitar start --service=services/standalone.json"
    },
    "dependencies": {
        "jitar": "*"
    }
}